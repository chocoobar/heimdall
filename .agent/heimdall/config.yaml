sre:
  user_name: "naren"
  output_folder: "_bmad-output"
  project_name: "test"
  language: "English"
  
  # SRE Artifacts output directory
  sre_artifacts: "_sre-artifacts"
  
  # MCP Configuration - Atlassian MCP integration
  mcp_jira_enabled: true
  mcp_confluence_enabled: false
  
  # Prometheus endpoint (optional - leave empty if not configured)
  prometheus_endpoint: ""